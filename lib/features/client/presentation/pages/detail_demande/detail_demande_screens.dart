import 'package:flutter/material.dart';

class DetailDemandeRejetScreen extends StatelessWidget {
  const DetailDemandeRejetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Détail Demande - Rejet - À implémenter')),
    );
  }
}

class DetailDemandeAttenteScreen extends StatelessWidget {
  const DetailDemandeAttenteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Détail Demande - En attente - À implémenter')),
    );
  }
}

class DetailDemandeEnCoursScreen extends StatelessWidget {
  const DetailDemandeEnCoursScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Détail Demande - En cours - À implémenter')),
    );
  }
}

class DetailDemandeValideScreen extends StatelessWidget {
  const DetailDemandeValideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Détail Demande - Valide - À implémenter')),
    );
  }
}

class DetailDemandeBrouillonScreen extends StatelessWidget {
  const DetailDemandeBrouillonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Détail Demande - Brouillon - À implémenter')),
    );
  }
}
