import 'package:flutter/material.dart';

class NouvelleDemandeStep1Screen extends StatelessWidget {
  const NouvelleDemandeStep1Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Nouvelle Demande Step 1 - À implémenter')),
    );
  }
}

class NouvelleDemandeStep2Screen extends StatelessWidget {
  const NouvelleDemandeStep2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Nouvelle Demande Step 2 - À implémenter')),
    );
  }
}

class NouvelleDemandeStep3Screen extends StatelessWidget {
  const NouvelleDemandeStep3Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Nouvelle Demande Step 3 - À implémenter')),
    );
  }
}

class NouvelleDemandeStep4Screen extends StatelessWidget {
  const NouvelleDemandeStep4Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Nouvelle Demande Step 4 - À implémenter')),
    );
  }
}

class NouvelleDemandeStep5Screen extends StatelessWidget {
  const NouvelleDemandeStep5Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Nouvelle Demande Step 5 - À implémenter')),
    );
  }
}

class NouvelleDemandeStep6Screen extends StatelessWidget {
  const NouvelleDemandeStep6Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Nouvelle Demande Step 6 - À implémenter')),
    );
  }
}
