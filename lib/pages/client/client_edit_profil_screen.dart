import 'package:flutter/material.dart';

class ClientEditProfilScreen extends StatelessWidget {
  const ClientEditProfilScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Client Edit Profil - À implémenter'),
      ),
    );
  }
}